[package]
name = "drain-gap"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "1"
drain-network = { path = "../drain-network" }
drain-corpus = { path = "../drain-corpus" }
drain-score = { path = "../drain-score" }
drain-tier = { path = "../drain-tier" }
